-f https://download.pytorch.org/whl/cu113/torch_stable.html
## NOTE: This repository is moving to astral's `uv` for dependency management.
## See `scripts/setup_uv_and_taskfile.sh` for a helper to install `uv` and a Taskfile runner.
## If you still want a direct pip requirements file, `uv` can export one; the loose minimums
## below are left for compatibility.
## (If you need GPU/CUDA-specific wheels add the appropriate -f index as required.)

## Optional CUDA wheel index (uncomment and edit if you need a specific CUDA build):
## -f https://download.pytorch.org/whl/cu113/torch_stable.html

gdown>=4.5
matplotlib>=3.5
numpy>=1.23
pandas>=1.5
opencv-python>=4.6
Pillow>=9.0
torch>=1.13
torchvision>=0.14
tqdm>=4.64

